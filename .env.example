# Password for the /admin panel (set this as a secret named ADMIN_PASSWORD)
ADMIN_PASSWORD=change-me

# Managed automatically by Lovable Cloud — no need to fill these in manually
# VITE_SUPABASE_URL=
# VITE_SUPABASE_PUBLISHABLE_KEY=
